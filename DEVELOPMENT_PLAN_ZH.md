# 基于 DESIGN_REQUIREMENTS_ZH.md 的开发计划（v1.0）

## 1. 文档信息

- **文档名称**：分布式大模型训练框架开发计划
- **输入基线**：`DESIGN_REQUIREMENTS_ZH.md`（当前版本）
- **目标仓库**：`disttrain`
- **计划周期**：建议 10 周（可按团队规模压缩或拉长）
- **交付形态**：可运行示例工程 + 配置模板 + 测试报告 + 性能报告 + 使用文档

---

## 2. 开发目标与策略

## 2.1 总目标

交付一个基于 PyTorch 的分布式训练框架示例，满足：
- 逻辑三阶段（Encoder / LLM / Decoder）且阶段可选启用；
- 每个启用阶段独立 TP/DP 与 Process Group；
- 支持多模态训练（图像/视频/语音输入，图像/语音输出）与纯文本 LLM 训练；
- 兼顾训练效率（吞吐、通信、显存）与算法易用性（配置、诊断、扩展）。

## 2.2 开发策略

采用“**先闭环、再扩展、后优化**”的增量式推进：

1. **先闭环**：优先打通 `llm only` 单阶段训练；
2. **再扩展**：扩展到两阶段、三阶段 Pipeline 与独立 TP/DP；
3. **后优化**：引入多模态插件、通信重叠、监控与性能调优。

这样可以尽早建立可运行基线，降低大范围并行调试风险。

---

## 3. 需求到工作包映射（WBS）

| 工作包 | 主要内容 | 对应需求 |
|---|---|---|
| WP-1 拓扑与配置系统 | enabled stages、world_size 校验、rank 映射、配置模板 | FR-01/02，架构 5.x |
| WP-2 分布式通信组 | stage/tp/dp PG 动态创建与生命周期管理 | FR-03/04 |
| WP-3 Pipeline 调度器 | GPipe + 1F1B，单/双/三阶段自动退化 | FR-06/07 |
| WP-4 训练引擎 | 前后向、梯度累积、优化器、混合精度、梯度同步 | FR-08/09 |
| WP-5 多模态插件 | image/video/audio encoder 分支、image/audio decoder 头 | FR-14/15 |
| WP-6 Checkpoint | 拓扑感知保存恢复、兼容校验 | FR-10/11 |
| WP-7 可观测性 | step/tokens/s/bubble ratio/通信耗时日志与指标 | FR-12/13 |
| WP-8 性能优化 | 异步 p2p、通信-计算重叠、重计算、I/O 优化 | PR 7.x |
| WP-9 测试与验收 | 功能矩阵、性能回归、稳定性与故障注入 | 12.x 验收标准 |
| WP-10 文档与示例 | 启动说明、FAQ、配置样例与最佳实践 | UR 8.x |

---

## 4. 里程碑计划（M0 ~ M6）

## M0：项目初始化（第 1 周）

**目标**
- 建立工程骨架与统一开发规范。

**任务**
- 创建目录结构（dist/configs/models/pipeline/train）。
- 搭建基础 CI（格式检查、单测入口、最小 smoke test）。
- 明确日志规范（普通文本 + JSON 可选）。

**产出**
- 初始工程骨架；
- `multimodal_tri_stage.yaml`、`text_llm_only.yaml` 配置草案；
- CI 脚本与开发规范文档。

**DoD（退出标准）**
- 本地与 CI 均可跑通基础检查。

---

## M1：拓扑与 PG 基础能力（第 2-3 周）

**目标**
- 实现可配置拓扑与分布式通信组管理，支持阶段可选启用。

**任务**
- 实现 `enabled_stages` 解析与 world_size 校验；
- 实现 rank 连续分配与 `tp_idx/dp_idx` 映射；
- 创建 stage/tp/dp process groups；
- 构建动态相邻阶段 p2p 通信组。

**产出**
- `dist/topology.py`、`dist/groups.py`、`dist/p2p.py`；
- 启动时拓扑摘要输出（阶段、rank、tp/dp、模态）。

**DoD**
- 支持 4 种拓扑：`llm only` / `encoder+llm` / `llm+decoder` / `encoder+llm+decoder`；
- 错配配置可启动前报错（含键路径与修复建议）。

---

## M2：训练闭环（单/双/三阶段） （第 4-5 周）

**目标**
- 打通训练核心路径，支持 GPipe/1F1B 调度和梯度更新。

**任务**
- 实现调度器（GPipe、1F1B）；
- 实现训练引擎（前后向、梯度累积、optimizer step）；
- 支持 BF16/FP16、梯度裁剪、DP 同步；
- 打通 `llm only` 与双/三阶段基础样例。

**产出**
- `pipeline/scheduler.py`、`pipeline/engine.py`；
- 单阶段与两阶段可运行 demo 配置。

**DoD**
- 1F1B 在 100+ steps 内稳定运行；
- 同配置下可切换 GPipe 与 1F1B；
- loss 与梯度曲线无异常发散（在 smoke 数据集上）。

---

## M3：多模态能力集成（第 6-7 周）

**目标**
- 支持图像/视频/语音输入与图像/语音输出的插件化集成。

**任务**
- 实现多模态 Encoder 插件注册机制；
- 实现 Decoder 多输出头与多任务损失加权；
- 统一多模态 batch 协议（text/image/video/audio）；
- 完成 `encoder + llm + decoder` 多模态训练链路。

**产出**
- `models/modalities/*` 插件样例；
- 多模态配置模板与启动说明。

**DoD**
- 支持至少 3 类输入模态（image/video/audio）中的任意组合；
- 支持 image/audio 输出头按配置启停；
- 新增一个视频分支无需改动调度核心即可接入。

---

## M4：工程化能力（第 8 周）

**目标**
- 增强可观测性、checkpoint 和容错诊断能力。

**任务**
- 增加 step 级指标（tokens/s、samples/s、显存峰值、bubble ratio）；
- 增加通信耗时与数据链路耗时指标（dataloader wait、H2D copy）；
- 实现 checkpoint 保存恢复（含拓扑元信息校验）；
- 增加典型故障报错与排查建议。

**产出**
- 指标采集模块与日志结构化输出；
- checkpoint 读写模块与恢复校验逻辑。

**DoD**
- checkpoint 恢复后 loss 连续；
- 日志可定位慢阶段与通信瓶颈 rank；
- 关键报错信息具备修复提示。

---

## M5：性能优化与回归（第 9 周）

**目标**
- 达到需求侧性能预期并稳定回归。

**任务**
- 异步 `isend/irecv` 与计算重叠；
- 梯度通信 bucket 化；
- activation checkpoint 策略分阶段调优；
- 视频/语音数据加载缓存与分片优化；
- 建立性能基线并回归测试。

**产出**
- 性能优化开关与推荐默认值；
- 性能报告（GPipe vs 1F1B、吞吐/显存/通信开销）。

**DoD**
- 1F1B 吞吐不低于 GPipe；
- 性能指标可复现，波动控制在约定范围内（建议 < 5%）。

---

## M6：验收与发布（第 10 周）

**目标**
- 完成验收矩阵、文档封版与示例发布。

**任务**
- 执行全量功能/性能/稳定性验收；
- 整理用户文档（快速开始、配置说明、FAQ、调优指南）；
- 输出发布清单与已知限制。

**产出**
- 验收报告；
- 发布版文档与示例配置；
- 版本标签与变更日志。

**DoD**
- 满足 `DESIGN_REQUIREMENTS_ZH.md` 第 12 章验收项；
- 无 P0/P1 阻塞问题。

---

## 5. 迭代排期（建议甘特视图文本版）

- **第 1 周**：M0 初始化
- **第 2-3 周**：M1 拓扑与 PG
- **第 4-5 周**：M2 训练闭环
- **第 6-7 周**：M3 多模态集成
- **第 8 周**：M4 工程化能力
- **第 9 周**：M5 性能优化
- **第 10 周**：M6 验收发布

并行建议：
- WP-1/2 与 WP-10 可并行；
- WP-5 多模态实现可在 M2 后半并行预研；
- WP-7/8 建议从 M2 起持续埋点，不要后置到末期。

---

## 6. 测试计划与验收矩阵

## 6.1 功能矩阵

| 维度 | 用例 |
|---|---|
| 拓扑组合 | `llm only` / `encoder+llm` / `llm+decoder` / `encoder+llm+decoder` |
| 并行配置 | 多组 `tp_size/dp_size`（含题设 16 卡） |
| 调度策略 | GPipe / 1F1B |
| 模态输入 | text / text+image / text+video / text+audio / 多模态组合 |
| 模态输出 | text / image / audio / image+audio |
| 恢复能力 | 中断恢复、拓扑不兼容恢复报错 |

## 6.2 性能与稳定性

- 连续训练稳定性：100+ steps（smoke）与更长稳态回归（nightly）。
- 性能对比：同配置下 GPipe vs 1F1B。
- 指标采集：step time、tokens/s、显存峰值、通信耗时、bubble ratio。
- 故障注入：通信超时、配置错误、checkpoint 不兼容。

## 6.3 质量门禁（建议）

- PR 门禁：lint + unit + 单机最小 e2e。
- 日构建门禁：多拓扑分布式回归 + checkpoint 恢复。
- 周构建门禁：性能回归与趋势报告。

---

## 7. 人员与角色建议（可按团队实际合并）

- **架构负责人（1）**：拓扑、调度、核心接口决策。
- **分布式工程师（1-2）**：PG、通信、性能优化。
- **训练引擎工程师（1-2）**：前后向、优化器、checkpoint。
- **多模态算法/工程师（1-2）**：模态分支与输出头接入。
- **测试与平台工程师（1）**：CI、自动化回归、指标平台接入。

---

## 8. 风险清单与应对

| 风险 | 触发信号 | 应对策略 |
|---|---|---|
| 阶段负载不均导致 pipeline 气泡大 | stage 耗时差距持续扩大 | 调整 micro-batch、重分配算子、优先优化慢阶段 |
| 跨阶段通信开销过高 | 通信占比高、GPU 利用低 | 启用异步 p2p、通信融合、拓扑感知映射 |
| 多模态 I/O 成瓶颈 | dataloader wait time 高 | 异步解码、缓存分片、预取与 pinned memory |
| 配置复杂导致误用 | 启动失败与配置错误频发 | 强校验、模板化配置、错误提示带修复建议 |
| checkpoint 兼容问题 | 恢复失败 | 保存拓扑元信息、恢复前强校验、提供迁移脚本 |

---

## 9. 交付物清单

1. 框架代码（拓扑、PG、调度、训练引擎、模态插件、checkpoint、监控）
2. 配置模板（`multimodal_tri_stage.yaml`、`text_llm_only.yaml` 等）
3. 测试资产（单测、e2e、性能回归脚本）
4. 报告文档（功能验收报告、性能报告、已知问题列表）
5. 使用文档（快速开始、配置手册、常见问题与调优建议）

---

## 10. 完成定义（Definition of Done）

满足以下条件视为计划完成：

1. 完成 `DESIGN_REQUIREMENTS_ZH.md` 中 FR/PR/UR 关键条目；
2. 4 类拓扑组合均可运行，且 `llm only` 与 16 卡三阶段多模态样例可复现；
3. checkpoint 恢复稳定、日志可诊断；
4. 1F1B 性能不低于 GPipe（同配置）；
5. 文档、模板、测试报告齐备并可交付。

---

## 11. 下一步执行建议（启动后 3 天内）

1. 冻结配置 schema（含 `enabled` 与模态字段）；
2. 优先完成 `topology.py/groups.py` 最小可运行版本；
3. 建立 `llm only` 的最小训练闭环作为主分支健康基线；
4. 同步搭建 e2e smoke 测试，避免后期集成爆炸。

